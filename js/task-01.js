const bodyEl = document.body;

const listEl = document.querySelectorAll(".item");

console.log(`Number of categories : ${listEl.length}`);

console.log(bodyEl.h2)

// listEl.forEach( element => console.log(element))

// console.log(listEl);